--- a/packages/client/ui-conversation/src/client/apply.ts
+++ b/packages/client/ui-conversation/src/client/apply.ts
@@ -401,7 +401,21 @@
         fileMentions: owner => ctx.get('chatFileMentions')?.forClosing(owner),
         openFile: (path) => {
           const cwd = sessions.list.getSnapshot().byId[sessionId]?.cwd
-          return workspaces.openPath(resolveWorkspacePath(cwd, path))
+          const resolved = resolveWorkspacePath(cwd, path)
+          // In the LDD desktop shell, documents render in the right-side preview
+          // panel instead of spawning the OS default application (which needs
+          // powershell.exe on Windows and can fail). Fall back to the native
+          // opener for files the panel cannot render (e.g. binaries).
+          const previewDocument = (globalThis as {
+            ldd?: { previewDocument?: (target: string) => Promise<{ shown: boolean }> }
+          }).ldd?.previewDocument
+          if (previewDocument !== undefined) {
+            return previewDocument(resolved).then((result) => {
+              if (result.shown) return
+              return workspaces.openPath(resolved)
+            })
+          }
+          return workspaces.openPath(resolved)
         },
         loadOlder: () => { void scoped.loadOlder() },
         loadImage: attachment => conversation.resolveImage(sessionId, attachment),
